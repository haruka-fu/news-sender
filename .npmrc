# セキュリティ設定

# package-lock.json を厳格に使用（サプライチェーン攻撃対策）
package-lock=true

# lockfileのみを使用してインストール（CI環境推奨）
# package-lockとpackage.jsonの不整合を検出
engine-strict=true

# 不審なスクリプト実行を防止
ignore-scripts=false

# パッケージの整合性チェックを強制
audit=true

# auditレベル設定（high以上で警告）
audit-level=high

# 公開レジストリのみを使用
registry=https://registry.npmjs.org/

# パッケージ署名の検証を有効化
sign-git-commit=false
